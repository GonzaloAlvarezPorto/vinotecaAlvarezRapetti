import React from 'react'

export const metadata = {
  title: "Vinoteca Alvarez Rapetti | Productos",
  description: "Página donde encontrarás todos los productos que podemos ofrecerte",
};

const layout = ({children}) => {

  return (
    <>{children}</>
  )
}

export default layout