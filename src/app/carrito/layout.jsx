import React from 'react'

export const metadata = {
  title: "Vinoteca Alvarez Rapetti | Carrito",
  description: "Finalizá tu compra",
};

const layout = ({children}) => {
  return (
    <>{children}</>
  )
}

export default layout